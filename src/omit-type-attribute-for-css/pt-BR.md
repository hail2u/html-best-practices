# Omita o atributo `type` para CSS

Em HTML, por padrão o atributo `type` do elemento `style` ja é `text/css`.

Ruim:

    <style type="text/css">
      ...
    </style>

Bom:

    <style>
      ...
    </style>
